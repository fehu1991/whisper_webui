# 變更紀錄

## 1.2.0 — 2026-09-05

- 新增 WhisperLiveKit 0.2.26 本機即時收音與獨立文字稿編輯頁，原有音檔轉錄保留。
- 原文字稿工具接收確認片段時只附加新行，不重建舊輸入欄位；保留人工修改與游標。
- 逐字稿自動儲存至本機，原始辨識與修改分開保存，可載入先前工作。
- 收音子程序隔離模型生命週期；與音檔轉錄共用工作互斥，停止時處理尾句。
- 新增即時部署入口與保密操作說明；正式啟動不自動安裝或下載，停用使用統計與外部暖機下載。
- 即時說話人辨識未啟用，可手動標註；原有 pyannote 音檔流程不變。

## 1.1.0 — 2026-08-08

### 新增

- 單檔轉錄狀態顯示目前已用時間與完成用時。
- 批次佇列顯示每個音檔的獨立用時，失敗檔也會保留已處理時間。
- 轉錄歷史保存並顯示每筆工作的用時。

## 1.0.0 — 2026-07-28

### 新增

- 泛用 Windows 環境檢測：CPU、記憶體、磁碟、NVIDIA／AMD／Intel GPU、驅動、CUDA、CTranslate2、PyTorch、模型與套件狀態。
- 桌面版環境、模型／快取與轉錄歷史管理頁。
- 依硬體動態建議的快速、平衡、高準確與 CPU 品質設定檔。
- 每個轉錄句段的 1-based 編號，跨畫面、TXT、JSON、SRT 與 VTT 對應。
- 單一推理資源工作管理、取消狀態、批次失敗重試。
- 原子輸出、歷史 manifest、模型容量與安全 wheel 快取清理。
- 完整基礎依賴鎖、CUDA PyTorch 鎖、發行檢查與 CPU／CUDA 試跑工具。

### 重構

- 將轉錄、說話人分離、說話者指派、輸出、歷史、儲存、環境與任務控制拆成獨立模組。
- `app.py` 保留 Gradio 桌面介面與應用流程，不再直接管理推理模型細節。
- 安裝、更新、診斷、修復與發行流程改為各自獨立入口。

### 相容性

- 僅支援 Windows 10／11 x64 桌面環境。
- 已驗證 Python 3.11、3.12 與 3.13；不提供手機或平板版。
- AMD／Intel GPU 目前走 CPU 推理，不宣稱支援 CUDA。
